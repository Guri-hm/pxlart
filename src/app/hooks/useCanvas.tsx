import { ColorType, PxlType, CurrentStrokeType, AllStrokesType, StorokesStrageType, BinarizationParamsType, NonogramAnswerType, PxlArrayType } from '@/app/components/strokeTypes';
import Utils from '../utils/utils';
import React, { useRef, useEffect, RefObject } from 'react';

export function useCanvas() {

    return {
    }
}